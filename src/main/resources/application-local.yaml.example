spring:
  datasource:
    username: pulse_user
    # db/init.sql의 로컬 개발 기본값과 동일. 개인적으로 바꿨다면 여기도 맞출 것.
    password: CHANGE_ME

pulse:
  jwt:
    # HS256 서명 키 — 32바이트(256비트) 이상 필요. 예: openssl rand -base64 48
    secret: CHANGE_ME_32_BYTES_MINIMUM
